<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_main_foreground"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TK88COLOR"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
          >

        </activity>
        <activity android:name=".ui.PhoneSplashActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.PoliciesPhoneActivity" />
        <activity android:name=".ui.PhoneSupportsActivity" />
        <activity android:name=".ui.IntroductionPhoneActivity" />
        <activity android:name=".ui.ParentPhoneActivity" />
        <activity android:name=".ui.home.ImageHomeActivity" />
        <activity android:name=".ui.home.ColorHomePickActivity" />
        <activity android:name=".ui.phone.PhoneSupActivity" />
    </application>

</manifest>